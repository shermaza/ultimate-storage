package com.ultimatestorage;

import lombok.extern.slf4j.Slf4j;
import net.runelite.api.Client;
import net.runelite.client.config.ConfigManager;
import net.runelite.client.game.ItemManager;
import net.runelite.client.ui.PluginPanel;

@Slf4j
public class UltimateStoragePanel extends PluginPanel {

    public UltimateStoragePanel(Client client, UltimateStorageConfig config,
                                ItemManager itemManager, ConfigManager configManager) {
        super();
    }
}
